import React from 'react';
import {NOT_FOUND} from '../constants/constants.js';
const NotFound = () => {
    return (
        <h1 className="text-center">{NOT_FOUND}</h1>
    )
};

export default NotFound;